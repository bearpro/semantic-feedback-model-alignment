Опиши на C# модель, описывающую данную вещь.

Требования:
- Верни только один C# файл.
- Не пиши пояснений вне кода.
- Используй типы, свойства и enum там, где считаешь нужным.
- Модель должна отражать сущности, их свойства и связи, явно присутствующие в исходном описании.
- Не добавляй сведения, которых нет в тексте.
- Не добавляй placeholder-поля, запасные поля, `Notes`, `AdditionalInfo`, `Metadata`, `Comment`, `Description`, если таких сущностей или атрибутов нет в исходном тексте.
- Не добавляй примерные комментарии `e.g.` и не вставляй пояснения в код без необходимости.
- Не присваивай значения по умолчанию только для того, чтобы продублировать текст; предпочитай структурную модель без избыточной инициализации.
- Если в тексте есть неопределённость, допускается nullable или комментарий TODO внутри кода.

Исходный текст:
---
{{SOURCE_TEXT}}
---
